\section{Related work}
\label{sec:related}

\TODO{draft. Four groups, in this order --- the last two exist to pre-empt specific reviews.}

\paragraph{Attention as inference.}
\citet{wu2023probabilistic} and the line of work deriving attention from probabilistic models
rather than positing it. \TODO{fill in; this is the direct parent of the paper}

\paragraph{Structured and syntax-aware language models.}
\TODO{grammar-induction and dependency-aware LMs; position this work as one where the structure
is latent and inferred, not supervised}

\paragraph{Weight sharing and looped transformers.}
\TODO{cite the looped-transformer line \citep{giannou2023looped, yang2024looped} and universal
transformers. Purpose: establish that the Looped baseline of \Cref{sec:exp2} is a recognised
model rather than a strawman built for this paper.}

\paragraph{The softmax bottleneck.}
\citet{yang2018breaking}. \TODO{connect to \Cref{sec:bottleneck}: our exact readout is a
mixture of softmaxes for structural reasons rather than as a fix for the bottleneck, and
whether it inherits the benefit is an empirical question we can answer.}
